# claude-knock MQTT ACL Configuration
#
# Hook user (publisher): can publish to any claude-knock topic
user claude-knock-hook
topic write claude-knock
topic write claude-knock/#

# Device users (subscribers): claude-knock-XXXXXX pattern
# Each device can read the shared topic
pattern read claude-knock
pattern read claude-knock/#
